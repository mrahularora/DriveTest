<header class="page-header">
  <div class="container px-4 px-lg-5">
    <div class="page-header-copy">
      <h1><%= heading %></h1>
      <% if (description) { %>
        <p><%= description %></p>
      <% } %>
    </div>
  </div>
</header>
